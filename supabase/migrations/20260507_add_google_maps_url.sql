-- =====================================================================
-- shops.google_maps_url 列の追加
-- 適用日: 2026-05-07
--
-- 背景:
--   店舗詳細パネルから「Google マップで開く」を可能にするため、
--   admin が任意で Google マップの URL を登録できるようにする。
--   未設定の場合は従来通り lat/lng のピンのみで動作する（パネルにも
--   ボタンを出さない）。
-- =====================================================================

ALTER TABLE public.shops
  ADD COLUMN IF NOT EXISTS google_maps_url text;
